// Fungsi enkripsi
export function encryptId(id) {
  // Implementasikan logika enkripsi sesuai kebutuhan
  return id.toString(); // Contoh sederhana, hanya mengonversi ke string
}

// Fungsi dekripsi
export function decryptId(encryptedId) {
  // Implementasikan logika dekripsi sesuai kebutuhan
  return parseInt(encryptedId, 10); // Contoh sederhana, mengonversi ke integer
}